import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  app: {
    backgroundColor: '#990007',
    minHeight: '100%',
    position: 'relative',
  },
});

export default styles;